# Heavy geo stack — only needed to regenerate data/generated/ from raw sources
# (make sources / base / trace). The site build, validation and geometry step
# run on the Python standard library alone.
shapely>=2.0
numpy>=1.26
scipy>=1.11
basemap>=1.4
basemap-data>=1.3
basemap-data-hires>=1.3
